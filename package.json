{
  "name": "soundenginecar-web",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "npx live-server --port=8080 --open=./index.html",
    "tailwind": "npx tailwindcss -i ./css/styles.css -o ./css/styles.min.css --watch"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "tailwindcss": "^3.4.4"
  }
}